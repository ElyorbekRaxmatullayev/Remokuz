/* ==========================================================================
   Remok.uz — настройки сайта

   Это единственный файл, который нужно править при смене телефона,
   бота или получателей заявок. main.js трогать не нужно.
   ========================================================================== */

window.REMOK_CONFIG = {

  /* Номер для WhatsApp и запасных ссылок (только цифры).
     Ссылки tel: в разметке правятся в index.html. */
  PHONE_DIGITS: '998909180501',


  /* ------------------------------------------------------------------------
     КУДА УХОДЯТ ЗАЯВКИ

     Есть два режима. Работает тот, который настроен.

     ── Режим 1: НАПРЯМУЮ В TELEGRAM (сейчас включён) ──────────────────
     ENDPOINT оставлен пустым, заявка уходит прямо из браузера
     в Telegram по токену ниже.

     Плюс:  ничего не нужно разворачивать, работает сразу.
     Минус: токен лежит в этом файле, а файл открыт всем посетителям сайта.
            Любой может открыть remok.uz/js/config.js и увидеть его.
            С этим токеном чужой человек сможет писать сообщения от имени
            вашего бота и читать входящие.

     ── Режим 2: ЧЕРЕЗ СВОЙ ОБРАБОТЧИК (безопасный) ────────────────────
     Впишите в ENDPOINT адрес своего Cloudflare Worker (файл worker.js
     в корне проекта, разворачивается за 5 минут и бесплатно), а TG_TOKEN
     здесь очистите. Тогда токен будет храниться на сервере, в переменных
     окружения, и в браузер не попадёт вообще.

     Пошаговая инструкция — в README.md, раздел «Безопасность токена».
     ------------------------------------------------------------------------ */

  // Режим 2: адрес обработчика, например 'https://remok-lead.xxx.workers.dev'
  // Пока пусто — работает режим 1.
  ENDPOINT: '',

  // Режим 1: токен бота @remokuz_bot
  TG_TOKEN: '8447297138:AAHvQ1AuSqHuaG8Ph7WiGuXJLR77zpMXJb4',

  /* Кому приходят заявки. Числовые ID, не @username.

     ⚠️ ВАЖНО: Telegram не разрешает боту писать первым.
     Каждый человек из списка должен сам открыть https://t.me/remokuz_bot
     и нажать «Начать» (Start). Пока он этого не сделал, заявки ему
     не дойдут — Telegram ответит «chat not found».

     Свой ID можно узнать у бота @userinfobot.
     Чтобы заявки шли в общую группу: добавьте бота в группу, сделайте
     администратором и впишите сюда ID группы (он начинается с минуса). */
  TG_CHAT_IDS: [
    '2077634702',
    '1430286964',
    '5884034743'
  ]

};
